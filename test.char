go/package main/go

use
	"fmt"

main
	main()
		var v, s = main.doThing(1 + 2 * 7, 2), "dood"
		main.test()
		go/fmt.Println(v, s)/go
		
	doThing(a, b int) int
		return a + b
	
	test() int
		return 42
